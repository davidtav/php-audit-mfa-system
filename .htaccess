RewriteEngine On

# Se o arquivo ou diretório existir, servir normalmente
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Senão, enviar para o front controller
RewriteRule ^ index.php [QSA,L]
